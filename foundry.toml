[profile.default]
src = "contracts"
out = "out"
script = "script"
test = "test"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "paris"

[rpc_endpoints]
ritual = "${RITUAL_RPC_URL}"

[etherscan]
# Add explorer API config here if/when Ritual Explorer supports verification
